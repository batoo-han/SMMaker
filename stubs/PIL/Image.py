class Image:
    LANCZOS = 1
    @staticmethod
    def open(fp):
        class Img:
            def thumbnail(self, size, resample):
                pass
            def save(self, buf, format=None, quality=None):
                pass
        return Img()
